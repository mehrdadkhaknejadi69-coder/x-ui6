# تغییرات این مرحله (فاز ۱ و ۲)

طبق برنامه‌ای که توافق شد، کار به فازها تقسیم شد تا سرویس زنده خراب نشود.
این فایل فقط شامل فاز ۱ (ورودی + ساختار داشبورد) و فاز ۲ (شارژ حجم توسط ادمین) است.
فاز ۳ (بازطراحی ساختار سابسکریپشن) و فاز ۴ (سخت‌شدن Railway) در مرحله‌ی بعد انجام می‌شود.

## ۱) صفحه‌ی ورود — بازطراحی کامل
- به‌جای طرح دو ستونه‌ی قبلی، یک کارت متمرکز در وسط صفحه با پس‌زمینه‌ی متحرک (mesh gradient).
- همان لوگوی بنفش شما، همان API لاگین (`/login`) — فقط ظاهر عوض شده.

## ۲) ساختار داشبورد — از تب‌های بالا به نوار کناری (Sidebar)
- ناوبری قبلی (تب‌های افقی بالای صفحه) کاملاً جمع شد.
- حالا یک ناوبری عمودی سمت راست دارید با آیکون، برچسب، و دسته‌بندی بخش‌ها
  (سرویس / فروش / مدیریت).
- بالای صفحه یک نوار باریک با عنوان صفحه‌ی جاری باقی مانده.
- در موبایل، نوار کناری به‌صورت کشویی (off-canvas) با دکمه‌ی همبرگری کار می‌کند.
- تمام صفحات قبلی (داشبورد، کانفیگ‌ها، دسته‌بندی‌ها، گروه‌های ساب، پلن‌ها،
  گزارش‌ها، ادمین‌ها، فعالیت‌ها، تنظیمات) دقیقاً با همان API قبلی کار می‌کنند —
  فقط پوسته و چیدمان تغییر کرده، منطق دست‌نخورده است.

## ۳) قابلیت جدید: «شارژ حجم» توسط ادمین
یک تب جدید در نوار کناری اضافه شد که به شما اجازه می‌دهد:
- یک کانفیگ را از لیست انتخاب کنید
- یک مقدار حجم (با دکمه‌های آماده‌ی ۱۰/۲۵/۵۰/۱۰۰ گیگ یا مقدار دلخواه) وارد کنید
- یک تعداد روز اضافه (اختیاری) وارد کنید
- با یک کلیک، این مقدار به سقف مصرف و تاریخ انقضای فعلیِ همان کانفیگ **اضافه**
  می‌شود (نه جایگزین) — یعنی اگر مثلاً بگویید «۵۰ گیگ شارژ کن»، حجم فعلی
  همان مشتری ۵۰ گیگ بیشتر می‌شود.
- اگر کانفیگ از قبل «نامحدود» باشد، افزودن حجم روی آن بی‌معنی است و پنل این
  را به شما اطلاع می‌دهد (فقط بخش روز، در صورت وارد کردن، اعمال می‌شود).
- این کار از طریق یک endpoint جدید و کاملاً مجزا در بک‌اند انجام می‌شود:
  `POST /api/links/{uid}/grant` با بدنه‌ی `{"volume_gb": 50, "days": 0}` —
  هیچ endpoint یا منطق قبلی تغییر نکرده، فقط این یکی اضافه شده است.

## نکات فنی
- هر دو فایل (`main.py` و `pages.py`) از نظر سینتکس پایتون به‌طور کامل بررسی
  و کامپایل شدند.
- تمام توابع جاوااسکریپتی که در HTML صدا زده می‌شوند (`onclick`, `onchange`, ...)
  و تمام آیدی‌های المان‌ها بررسی شدند تا هیچ‌کدام گم نشده باشند.
- منطق relay/xhttp/پرداخت/ربات فروش کاملاً دست‌نخورده مانده است.

## فاز بعدی (طبق برنامه)
- فاز ۳: بازطراحی ساختار سابسکریپشن (صفحه‌ی عمومی مشتری `/subscription/{uuid}`
  و گروه‌های ساب).
- فاز ۴: بررسی و سخت‌کردن سازگاری کامل با Railway (TCP Proxy، env varها).

اگر آماده‌اید، فاز بعدی رو شروع می‌کنم.
